import {actions as posts} from './posts.reducer'
import {actions as comments} from './comments.reducer'
import {actions as categories} from './categories.reducer'

export const actions = {
  posts,
  categories,
  comments,
};
